

export const isProductionEnvironment = (environment: string) => environment === 'production';
export const isDevelopmentEnvironment = (environment: string) => environment === 'development';
export const isLocalEnvironment = (environment: string) => environment === 'local';
